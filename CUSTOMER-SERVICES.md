# الشحنات مجهولة المالك وطلبات التوصيل

إضافة 2026-09-13، على نفس التطبيق والاستضافة وتسجيل الدخول الحاليين.

## الاستخدام

- الإدارة / الموظف المسؤول / موظف المخزن: قسم «مجهولة المالك». تسجيل وصف وتاريخ الاستلام، رقم تتبع حقيقي اختياري، وصورة JPG/PNG/WebP حتى 5 ميجابايت. لا يتم إنشاء كود زبون أو شحنة عادية أثناء التسجيل.
- الربط: اختيار زبون موجود ومفعّل، رحلة محفوظة لم تغادر، رقم تتبع حقيقي ووزن وحالة فعلية، ثم توثيق كيفية التأكد من الملكية والتأكيد. تظهر الشحنة وصورتها للزبون بعد نجاح العملية كاملة فقط.
- لا يُستبدل تتبع موجود ولا يُعاد ربط السجل بمالك آخر من القائمة. تبقى السجلات المرتبطة في فلتر «تم الربط». تعديل الصورة بعد الربط من قسم الشحنات العادية.
- الزبون: «طلبات التوصيل» ثم «طلب توصيل للبيت»، أو زر التوصيل داخل تفاصيل الرحلة. يختار شحناته، يدخل الهاتف والعنوان والملاحظات الاختيارية.
- الإدارة / الموظف المسؤول: قسم «طلبات التوصيل» مستقل، يبدأ بفلتر الطلبات الجديدة. يمكن بدء التنسيق، إرسال رد داخل الطلب، إلغاء الطلب، ثم إغلاقه. لتصفّح المنسّق أو المغلق غيّر فلتر الحالة.
- الزبون يرى طلباته ورد الشركة فقط، ويمكنه إلغاء الطلب الجديد قبل بدء التنسيق.
- إغلاق طلب التوصيل لا يغيّر حالة الشحنات إلى «تم التسليم». يجب تحديثها بعد التسليم الفعلي بالطريقة المعتادة.
- لا رسوم مفترضة ولا موعد توصيل مضمون ولا رسالة واتساب تلقائية. الطلب المسبق مسموح قبل جاهزية الشحنات، مع التنسيق الفعلي لاحقًا.

## الحماية وسلامة البيانات

- قوائم مجهولة المالك وصورها غير متاحة للزبائن أو المحاسب. عناوين التوصيل متاحة للزبون صاحب الطلب والإدارة والموظف المسؤول فقط، وليس موظف المخزن أو المحاسب.
- التحقق من الجلسة وOrigin في Worker قبل جميع المسارات الجديدة. لا تُؤخذ هوية الزبون من محتوى الطلب؛ تأخذها قاعدة البيانات من هوية الجلسة الموثّقة.
- RLS مفعّل مع سحب صلاحيات PUBLIC/anon/authenticated. الجداول والدوال متاحة لخدمة الخادم فقط. جميع الدوال SECURITY INVOKER مع search_path فارغ.
- الربط وحجز الشحنات للتوصيل عمليتان ذريتان. قفل الشحنات بترتيب ثابت وفهرس جزئي فريد يمنعان طلبين مفتوحين لنفس الشحنة.
- معرّفات الطلبات تدعم إعادة المحاولة بلا تكرار. التعديلات والصور تستخدم updated_at لمنع الكتابة فوق تعديل أحدث.
- إعادة تسمية / حذف / تغيير مالك شحنة لها طلب مفتوح محظور. روابط السجلات التاريخية تمنع حذف الشحنات المرجعية؛ لا تحذف تاريخ طلبات التوصيل من الواجهة.
- الصور عبر R2 ومسارات خاصة تتطلب جلسة. حد الصورة 5 ميجابايت مع فحص التوقيع، وحجز الحصة بالطريقة المعتمدة في التطبيق. لا تُحذف صورة على فشل غير مؤكد في استجابة قاعدة البيانات.
- إذا حُفظ السجل وفشل رفع الصورة تُعرض رسالة واضحة، ويمكن إعادة رفع الصورة من السجل بدون تسجيل شحنة جديدة.
- لا توجد بيانات عميل أو أسرار في ملفات الإضافة أو اختبارات المستودع.

## قاعدة البيانات والاختبارات

طبّق بالترتيب بعد عمليات التطبيق السابقة:

1. `db/customer-services.sql`
2. `db/customer-services-link-fix.sql` (تصحيح تعارض تسمية متغير التتبع المكتشف أثناء الاختبار).

تم تطبيقهما في Supabase باستخدام migrations؛ لا تُعد تشغيل ملفات CREATE على قاعدة حُدّثت بالفعل.

الاختبارات: `node --test tests/*.test.mjs`، وملفا `tests/database-rollback.sql` و`tests/customer-services-rollback.sql`. كلا ملفي SQL يستخدم بيانات مصطنعة ومعاملة ROLLBACK. الاختبارات الجديدة تعمل كـ service_role وتتحقق من العزل والملكية والربط والصورة ومنع التكرار والإلغاء والتعديلات القديمة والتدقيق.

الفحص الأمني: RLS بدون سياسات مقصود لأن الدخول عبر خدمة الخادم فقط. تحذير Supabase Auth عن فحص كلمات المرور المسربة سابق للإضافة، ولا يخص تسجيل الدخول المخصص الحالي. مرجع: https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection

## النشر والرجوع

يُنشر تحديث ملفات الجذر معًا عبر GitHub إلى Cloudflare Pages. تُحفظ إعدادات wrangler والأسرار وR2 دون تغيير. مسارات ملفات الخادم وSQL والاختبارات محجوبة من الموقع.

للرجوع الطارئ، أعد إصدار ملفات التطبيق السابق فقط، مع إبقاء الجداول والسجلات الجديدة لحماية طلبات الزبائن. لا تحذف جداول أو صورًا. تعطيل الواجهة لا يلغي الطلبات المفتوحة؛ تُراجع قبل تغيير ملكية شحنة مرتبطة بطلب.
